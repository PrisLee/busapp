# Collected data: the local record, rebuilt by running the app.
data/busapp.sqlite3
data/busapp.sqlite3-wal
data/busapp.sqlite3-shm

# Derived from BusRouter SG; regenerated by scripts/fetch_network.py.
# Not committed, so it can never go stale against LTA's live route changes.
data/network.json

# Secrets
.env

__pycache__/
*.py[cod]
.DS_Store
